# CPU stack — use for development machines without NVIDIA CUDA.
# Keeps torch/torchvision on explicit +cpu wheels so they do not pick up CUDA builds from PyPI.
# Requires: pip >= 21; Python 3.10+ (match your team’s interpreter).
--extra-index-url https://download.pytorch.org/whl/cpu

dji-thermal-sdk==0.0.2
fastapi==0.135.3
imageio-ffmpeg==0.6.0
numpy==2.4.4
opencv-python-headless==4.13.0.92
pillow==12.2.0
python-multipart==0.0.24
python-socketio==5.16.1
requests==2.33.1
sahi==0.11.36
ultralytics==8.4.36
uvicorn[standard]==0.44.0

torch==2.11.0+cpu
torchvision==0.26.0+cpu
